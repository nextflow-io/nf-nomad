nextflow_workflow {

    name "Test Constraints"
    script "workflows/hello.nf"
    config "tests/constraints.config"
    workflow "MAIN"

    test("Should run without failures") {

        when {
            params {
            }
            workflow {
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

}
