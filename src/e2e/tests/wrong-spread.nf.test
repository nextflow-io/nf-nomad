nextflow_workflow {

    name "Test Wrong Spread"
    script "workflows/hello.nf"
    config "tests/wrong-spread-nextflow.config"
    workflow "MAIN"

    test("Should run without failures") {

        when {
            params {
            }
            workflow {
            }
        }

        then {
            assert workflow.success == false
        }

    }

}
