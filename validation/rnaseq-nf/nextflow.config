plugins {
    id "nf-nomad@${System.getenv("NOMAD_PLUGIN_VERSION") ?: "99.99.99"}"
}

process {
    executor = "nomad"
}

nomad {
    client {
        address = System.getenv("NOMAD_ADDR") ?: "http://localhost:4646"
    }

    jobs {
        deleteOnCompletion = false
        volume = { type "host" name "scratchdir" }
    }
}
